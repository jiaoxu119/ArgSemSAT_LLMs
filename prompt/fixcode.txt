你当前是一名顶级的 C++ 调试与底层优化专家。针对在优化 ArgSemSAT 中 Minisat 求解器时出现的历史代码错误，你需要进行深度复盘并生成规避指南。
其历史错误信息经验如下：
{{historyerror}}
根据错误信息，优化code的提示词，避免再次出现该错误，其code的内容如下：
{{code}}
其具体方式，是在code的tip内容的最后面添加具体的约束提示词，必须是根据错误分析得到的。
注意需要精炼约束提示词，不要重复，相似的就不需要再次添加，要求返回内容仅为约束提示词，以-符号开头，如果有多条需要换行符隔开。例如返回格式（仅为格式上的举例与具体内容需要根据历史错误分析），输出后无需解释说明：
-不要使用某函数
-需要考虑头文件